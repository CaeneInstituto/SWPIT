# ─── Culqi - Claves de PRUEBA (TEST) ─────────────────────────────────────────
# Copia este archivo como .env y reemplaza con tus claves reales
# Obtén tus claves en: https://dashboard.culqi.com → Desarrollo → API Keys

# Clave PÚBLICA (va en el frontend - empieza con pk_test_ o pk_live_)
VITE_CULQI_PUBLIC_KEY=pk_test_foBlu9NBJohtJoob

# Clave SECRETA (solo en el backend - empieza con sk_test_ o sk_live_)
# Esta se configura en Vercel → Settings → Environment Variables
CULQI_SECRET_KEY=sk_test_k7itVpsPROqWlzkc

# URL del servidor backend (déjalo vacío o comentado para producción en Vercel)
# Solo úsalo para desarrollo local si tienes el backend corriendo en otro puerto
# VITE_API_URL=http://localhost:3001
